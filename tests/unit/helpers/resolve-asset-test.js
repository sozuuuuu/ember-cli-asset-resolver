import { resolveAsset } from '../../../helpers/resolve-asset';
import { module, test } from 'qunit';

module('Unit | Helper | resolve asset');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resolveAsset(42);
  assert.ok(result);
});
